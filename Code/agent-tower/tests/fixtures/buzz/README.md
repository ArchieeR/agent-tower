# Buzz native fixtures

`organization-export-v1.e65ce617e.json` is copied byte-for-byte from Buzz PR #6419 branch commit `e65ce617e`, path `desktop/src-tauri/tests/fixtures/organization-export-v1.json`.

The Buzz Rust test serializes the single pure exporter and asserts exact JSON equality. Keep the commit suffix when updating so contract review can identify the precise native producer revision.
